# Інструкція з логіну в мобільний додаток

## 🔐 Система авторизації

Мобільний додаток використовує **адмін-панель** для авторизації:
- **URL:** `https://admin.foryou-realestate.com/api`
- **Endpoint:** `/auth/login`

## 📝 Тестові дані для входу

### Варіант 1: Тестовий інвестор (якщо створено)

**Email:** `investor@test.com`  
**Password:** `Investor123!`

> ⚠️ **Примітка:** Інвестор має обмежений доступ до CRM. Для повного доступу до CRM потрібна роль **BROKER** або **ADMIN**.

### Варіант 2: Створити нового користувача

Якщо у вас немає облікового запису, ви можете:

1. **Зареєструватися через додаток:**
   - Натисніть "Sign up" на екрані логіну
   - Оберіть тип реєстрації (Investor, Agent/Broker)
   - Заповніть форму реєстрації

2. **Або створити через API:**

```bash
# Для брокера (повний доступ до CRM)
curl -X POST https://admin.foryou-realestate.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "broker@test.com",
    "password": "Broker123!",
    "firstName": "Broker",
    "lastName": "Test",
    "role": "BROKER",
    "phone": "+971501234567"
  }'
```

Після реєстрації через API, потрібно активувати користувача в базі даних (якщо статус `PENDING`).

## 🎯 Доступ до CRM

Для доступу до CRM потрібна роль:
- **BROKER** - повний доступ до leads та CRM
- **ADMIN** - повний доступ до всього
- **INVESTOR** - обмежений доступ (може не бачити всі leads)

## 🔧 Якщо виникає помилка "Користувач не авторизований"

1. **Перевірте, чи ви увійшли:**
   - Перейдіть на інші вкладки (Home, Properties)
   - Якщо вони працюють, значить ви авторизовані

2. **Перевірте токен:**
   - Токен зберігається в SecureStore після логіну
   - Якщо токен втрачено, потрібно увійти знову

3. **Перевірте роль користувача:**
   - CRM доступний тільки для BROKER та ADMIN
   - INVESTOR може не мати доступу до всіх функцій CRM

## 📱 Як увійти

1. Відкрийте додаток
2. Якщо ви не авторизовані, ви побачите екран "Потрібна авторизація"
3. Натисніть кнопку "Увійти"
4. Введіть:
   - **Email** (або email/phone, залежно від системи)
   - **Password**
5. Натисніть "Sign in"

## 🆘 Якщо не маєте облікового запису

1. Натисніть "Sign up" на екрані логіну
2. Оберіть тип реєстрації:
   - **Investor** - для інвесторів
   - **Agent/Broker** - для брокерів (рекомендовано для CRM)
3. Заповніть форму та зареєструйтеся
4. Після реєстрації увійдіть з новими даними

## ⚙️ Технічні деталі

- **Авторизація:** Адмін-панель (`admin.foryou-realestate.com`)
- **CRM API:** Локальний бекенд (`foryou-realestate.com/api/v1`)
- **Токен:** Зберігається в SecureStore після успішного логіну
- **Автоматичне оновлення:** Токен автоматично оновлюється при застаріванні

