# ✅ Правильні налаштування Redirect URI в AMO CRM

## 📋 Що має бути в налаштуваннях

### Redirect URI (Ссылка для перенаправления)

**Правильний URL:**
```
https://admin.foryou-realestate.com/api/amo-crm/callback
```

**НЕ використовуйте:**
- ❌ `https://foryou-realestate.com/api/v1/integrations/amo-crm/callback` (це main backend)
- ❌ `https://admin.foryou-realestate.com/api/ar...` (неповний URL)

---

## 🔍 Що я бачу на скріншоті

На скріншоті два URL поля:

1. **Перше поле (обрізано):** `https://admin.foryou-realestate.com/api/ar...`
   - Має бути: `https://admin.foryou-realestate.com/api/amo-crm/callback`
   - ✅ Правильний домен (admin.foryou-realestate.com)
   - ⚠️ Потрібно перевірити повний URL

2. **Друге поле (обрізано):** `https://foryou-realestate.com/api/v1/integ...`
   - Має бути: `https://admin.foryou-realestate.com/api/amo-crm/callback`
   - ❌ Неправильний домен (foryou-realestate.com замість admin.foryou-realestate.com)
   - ❌ Неправильний шлях (/api/v1/integrations замість /api/amo-crm)

---

## ✅ Правильні налаштування

### Поле "Ссылка для перенаправления" (Redirect link):

```
https://admin.foryou-realestate.com/api/amo-crm/callback
```

**Важливо:**
- ✅ Домен: `admin.foryou-realestate.com` (не `foryou-realestate.com`)
- ✅ Шлях: `/api/amo-crm/callback` (не `/api/v1/integrations/amo-crm/callback`)
- ✅ Протокол: `https://` (не `http://`)

---

## 🔧 Як виправити

1. **В полі "Ссылка для перенаправления" введіть:**
   ```
   https://admin.foryou-realestate.com/api/amo-crm/callback
   ```

2. **Переконайтеся, що:**
   - URL повний (не обрізаний)
   - Немає зайвих символів
   - Протокол `https://`

3. **Натисніть "Сохранить" (Save)**

---

## 🧪 Перевірка після збереження

Після оновлення Redirect URI:

1. Спробуйте знову авторизуватися в AMO CRM з мобільного додатку
2. Після авторизації AMO CRM має перенаправити на:
   ```
   https://admin.foryou-realestate.com/api/amo-crm/callback?code=...&state=...
   ```
3. Backend автоматично перенаправить на:
   ```
   foryoure://amo-crm/callback?code=...&state=...
   ```
4. Мобільний додаток обробить deep link

---

## ⚠️ Якщо є два поля для URL

Якщо в налаштуваннях AMO CRM є два поля для URL:

1. **Redirect URI / Ссылка для перенаправления:**
   ```
   https://admin.foryou-realestate.com/api/amo-crm/callback
   ```

2. **Інше поле (якщо є):**
   - Може бути для webhook або інших цілей
   - Уточніть в документації AMO CRM

---

## 📋 Чеклист

- [ ] Redirect URI: `https://admin.foryou-realestate.com/api/amo-crm/callback`
- [ ] URL повний (не обрізаний)
- [ ] Використовується правильний домен (`admin.foryou-realestate.com`)
- [ ] Використовується правильний шлях (`/api/amo-crm/callback`)
- [ ] Налаштування збережено
- [ ] Протестовано авторизацію

---

**Останнє оновлення:** Січень 2025
