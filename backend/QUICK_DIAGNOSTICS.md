# 🔍 Швидка діагностика проблем

## 🚨 500 помилка при логіні

### Швидка перевірка (в порядку пріоритету):

1. **PostgreSQL контейнер запущений?**
   ```bash
   docker-compose ps postgres
   # Або
   docker ps | grep postgres
   ```
   **Якщо ні:** `docker-compose up -d postgres`

2. **Backend підключений до БД?**
   ```bash
   # Перевірити логи backend
   docker-compose logs backend | grep -i "database\|connection\|error"
   ```
   **Якщо помилки підключення:** Перевірте `DATABASE_URL` в `.env`

3. **Endpoint існує?**
   ```bash
   curl -X POST https://admin.foryou-realestate.com/api/auth/login \
     -H "Content-Type: application/json" \
     -d '{"email":"test@test.com","password":"test"}' \
     -v
   ```

4. **Користувач існує в БД?**
   ```sql
   SELECT * FROM users WHERE email = 'broker1@test.com';
   ```

---

## 🔧 Типові проблеми та рішення

### 1. PostgreSQL контейнер зупинений
**Симптоми:** 500 помилка, помилки підключення до БД

**Рішення:**
```bash
docker-compose up -d postgres
docker-compose restart backend
```

### 2. Неправильний DATABASE_URL
**Симптоми:** Помилки підключення до БД

**Рішення:**
```env
# Використовуйте ім'я контейнера або localhost
DATABASE_URL=postgresql://user:password@postgres:5432/database
# або
DATABASE_URL=postgresql://user:password@localhost:5432/database
```

### 3. Endpoint не існує
**Симптоми:** 404 помилка

**Рішення:** Перевірте, чи route підключено в `server.ts`

### 4. Неправильний формат відповіді
**Симптоми:** Мобільний додаток не може обробити відповідь

**Рішення:** Перевірте формат:
```json
{
  "success": true,
  "data": {
    "token": "...",
    "user": {...}
  }
}
```

---

**Останнє оновлення:** Січень 2025
