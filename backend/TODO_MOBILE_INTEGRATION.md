# 📋 Список завдань для підключення до мобільного додатка

## ✅ Що вже підключено

- ✅ Авторизація (auth.ts)
- ✅ Нерухомість (properties.ts)
- ✅ Улюблені (favorites.ts)
- ✅ Девелопери (developers.ts)
- ✅ Курси (courses.ts)
- ✅ Новини (news.ts)
- ✅ Нотифікації (notifications.ts)
- ✅ AMO CRM - читання (amo-crm.ts) - тільки pipelines/stages
- ✅ Leads - частково (leads.ts) - тільки читання
- ✅ Documents API - частково (expo-document-picker встановлений, використовується в support.tsx)

---

## 🔴 ВИСОКИЙ ПРІОРИТЕТ

### 1. Documents API - Завершити інтеграцію

**Статус бекенду:** ✅ Готовий та працює

**Що вже зроблено:**
- ✅ `expo-document-picker` встановлений
- ✅ Використовується в `support.tsx` для прикріплення файлів

**Що потрібно зробити:**

#### 1.1. Створити API клієнт
- [ ] Створити файл `mobile/api/documents.ts`
- [ ] Додати типи: `DocumentType`, `DocumentCategory`, `Document`, `UploadDocumentDto`, `UpdateDocumentDto`
- [ ] Реалізувати методи:
  - [ ] `upload()` - завантаження з `expo-document-picker`
  - [ ] `getByEntity()` - отримати документи для сутності
  - [ ] `getById()` - отримати документ
  - [ ] `update()` - оновити метадані
  - [ ] `delete()` - видалити документ
  - [ ] `verify()` - верифікувати (ADMIN)
  - [ ] `getAll()` - всі документи з фільтрами (ADMIN)

#### 1.2. Створити UI компоненти
- [ ] `mobile/components/documents/DocumentList.tsx` - список документів
- [ ] `mobile/components/documents/UploadDocumentModal.tsx` - модальне вікно завантаження

#### 1.3. Інтегрувати в екрани
- [ ] Додати в профіль користувача (`mobile/app/profile.tsx`)
- [ ] Додати в деталі нерухомості (`mobile/app/properties/[id].tsx`)
- [ ] Додати в деталі lead (`mobile/app/lead/[id].tsx`)

#### 1.4. Обробити формат відповіді
- [ ] Всі методи мають використовувати `response.data.data` (не `response.data`)
- [ ] Бекенд повертає `{ success: true, data: ... }`

**Документація:** `backend/DOCUMENTS_API_MOBILE_INTEGRATION.md`

**Очікуваний час:** 3-4 години (залежності вже встановлені)

---

### 2. Broker Clients API (CRM)

**Статус бекенду:** ✅ Готовий

**Що потрібно зробити:**

#### 2.1. Створити API клієнт
- [ ] Створити файл `mobile/api/broker-clients.ts`
- [ ] Додати типи: `BrokerClient`, `CreateBrokerClientDto`
- [ ] Реалізувати CRUD методи:
  - [ ] `create()` - створити клієнта
  - [ ] `getAll()` - список клієнтів
  - [ ] `getById()` - деталі клієнта
  - [ ] `update()` - оновити клієнта
  - [ ] `delete()` - видалити клієнта

#### 2.2. Створити UI компоненти
- [ ] `mobile/components/crm/BrokerClientsList.tsx` - список клієнтів
- [ ] `mobile/components/crm/BrokerClientForm.tsx` - форма створення/редагування
- [ ] `mobile/components/crm/BrokerClientDetails.tsx` - деталі клієнта

#### 2.3. Створити екран CRM
- [ ] `mobile/app/crm/clients.tsx` - екран управління клієнтами
- [ ] Додати навігацію в CRM секцію

**Backend endpoints:**
- `POST /api/v1/broker-clients`
- `GET /api/v1/broker-clients`
- `GET /api/v1/broker-clients/:id`
- `PUT /api/v1/broker-clients/:id`
- `DELETE /api/v1/broker-clients/:id`

**Очікуваний час:** 3-4 години

---

### 3. Leads API - Повна інтеграція

**Статус бекенду:** ✅ Готовий

**Що потрібно зробити:**

#### 3.1. Оновити API клієнт
- [ ] Оновити файл `mobile/api/leads.ts`
- [ ] Додати методи:
  - [ ] `create()` - створити lead
  - [ ] `update()` - оновити lead
  - [ ] `assign()` - призначити брокера (BROKER, ADMIN)
  - [ ] `take()` - взяти lead з пулу (BROKER)
  - [ ] `updateStatus()` - змінити статус (BROKER, ADMIN)

#### 3.2. Оновити UI
- [ ] Додати форму створення lead
- [ ] Додати форму редагування lead
- [ ] Додати кнопку "Взяти lead" для брокерів
- [ ] Додати вибір статусу

**Backend endpoints:**
- `POST /api/v1/leads` - Створити lead
- `PUT /api/v1/leads/:id` - Оновити lead
- `POST /api/v1/leads/:id/take` - Взяти lead (BROKER)
- `POST /api/v1/leads/:id/assign/:brokerId` - Призначити брокера (ADMIN)

**Очікуваний час:** 2-3 години

---

## 🟡 СЕРЕДНІЙ ПРІОРИТЕТ

### 4. Analytics API

**Статус бекенду:** ✅ Готовий

**Що потрібно зробити:**

#### 4.1. Створити API клієнт
- [ ] Створити файл `mobile/api/analytics.ts`
- [ ] Додати типи для статистики
- [ ] Реалізувати методи:
  - [ ] `getDashboardStats()` - загальна статистика (ADMIN)
  - [ ] `getStatsByPeriod()` - статистика за період (ADMIN)
  - [ ] `getBrokerStats()` - статистика брокера

#### 4.2. Створити UI
- [ ] Dashboard екран з метриками
- [ ] Графіки та діаграми
- [ ] Фільтри по періоду

**Backend endpoints:**
- `GET /api/v1/analytics/dashboard` (ADMIN)
- `GET /api/v1/analytics/period?startDate&endDate` (ADMIN)
- `GET /api/v1/analytics/broker/:brokerId`

**Примітка:** Потрібно тільки для BROKER та ADMIN

**Очікуваний час:** 3-4 години

---

## 🟢 НИЗЬКИЙ ПРІОРИТЕТ

### 5. Activity Logs

**Статус бекенду:** ✅ Готовий

**Примітка:** Це потрібно тільки для адмін-панелі, не для мобільного додатка користувачів.

---

### 6. Data Sync

**Статус бекенду:** ✅ Готовий

**Примітка:** Це потрібно тільки для адмін-панелі, не для мобільного додатка користувачів.

---

## 📊 Пріоритети та очікуваний час

### Високий пріоритет (8-11 годин):
1. **Documents API (завершити)** - 3-4 години (залежності вже встановлені)
2. **Broker Clients API** - 3-4 години
3. **Leads API (повна інтеграція)** - 2-3 години

### Середній пріоритет (3-4 години):
4. **Analytics API** - 3-4 години

### Низький пріоритет:
5. Activity Logs - не потрібно для мобільного додатка
6. Data Sync - не потрібно для мобільного додатка

---

## 🎯 Рекомендований порядок виконання

### Фаза 1: Documents API (завершити інтеграцію)
1. Створити API клієнт (`mobile/api/documents.ts`)
2. Створити UI компоненти
3. Інтегрувати в екрани (профіль, property details, lead details)
4. Протестувати

### Фаза 2: Broker Clients API
1. Створити API клієнт
2. Створити UI компоненти
3. Створити екран CRM
4. Протестувати

### Фаза 3: Leads API (повна інтеграція)
1. Оновити API клієнт
2. Оновити UI для створення/редагування
3. Додати функціонал призначення брокерів
4. Протестувати

### Фаза 4: Analytics API (опціонально)
1. Створити API клієнт
2. Створити Dashboard UI
3. Додати графіки
4. Протестувати

---

## 📝 Детальні інструкції

### Documents API
**Документ:** `backend/DOCUMENTS_API_MOBILE_INTEGRATION.md`
- Повний код API клієнта
- UI компоненти з прикладами
- Інтеграція в екрани
- Тестування

### Broker Clients API
**Документ:** `backend/MOBILE_API_INTEGRATION_STATUS.md` (розділ "Broker Clients API")
- Структура API клієнта
- Типи та інтерфейси
- Приклади використання

### Leads API
**Документ:** `backend/MOBILE_API_INTEGRATION_STATUS.md` (розділ "Leads API - повна інтеграція")
- Методи для створення/оновлення
- Призначення брокерів
- Зміна статусів

---

## ✅ Чеклист для кожного модуля

Для кожного нового модуля потрібно:

- [ ] Створити API клієнт (`mobile/api/[module].ts`)
- [ ] Додати типи та інтерфейси
- [ ] Реалізувати всі методи
- [ ] Обробити формат відповіді (`response.data.data`)
- [ ] Обробити помилки
- [ ] Створити UI компоненти (якщо потрібно)
- [ ] Інтегрувати в екрани
- [ ] Протестувати всі сценарії
- [ ] Додати обробку помилок
- [ ] Додати loading states
- [ ] Додати валідацію (якщо потрібно)

---

## 🔗 Корисні посилання

- **Backend Swagger:** https://admin.foryou-realestate.com/api/docs
- **Documents API Guide:** `backend/DOCUMENTS_API_MOBILE_INTEGRATION.md`
- **Integration Status:** `backend/MOBILE_API_INTEGRATION_STATUS.md`
- **Expo Document Picker:** https://docs.expo.dev/versions/latest/sdk/document-picker/

---

**Останнє оновлення:** Грудень 2025
