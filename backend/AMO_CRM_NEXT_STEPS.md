# 🚀 Наступні кроки після налаштування URL в AMO CRM

## ✅ Що вже зроблено

1. ✅ Налаштовано Redirect URI: `https://admin.foryou-realestate.com/api/amo-crm/callback`
2. ✅ Налаштовано друге поле: `https://admin.foryou-realestate.com/api/amo-crm/disconnect`

---

## 🧪 Що потрібно протестувати

### 1. Тестування OAuth flow

**Крок 1:** Відкрийте мобільний додаток та перейдіть на CRM сторінку

**Крок 2:** Натисніть кнопку "Підключити AMO CRM"

**Очікуваний результат:**
- Відкривається браузер з формою авторизації AMO CRM
- Після авторизації AMO CRM перенаправляє на `https://admin.foryou-realestate.com/api/amo-crm/callback?code=...`
- Backend обробляє callback та перенаправляє на deep link `foryoure://amo-crm/callback?code=...`
- Мобільний додаток отримує callback та обмінює code на токени
- Статус AMO CRM оновлюється на "Підключено"

---

## 🔍 Що перевірити, якщо щось не працює

### Проблема: Callback не перенаправляє на deep link

**Симптоми:**
- Після авторизації в AMO CRM користувач залишається на сторінці backend
- Мобільний додаток не отримує callback

**Рішення:**
1. Перевірте, що callback endpoint використовує `res.redirect()` або HTML redirect
2. Див. `AMO_CRM_CALLBACK_REDIRECT_FIX.md` для деталей

---

### Проблема: 404 на callback endpoint

**Симптоми:**
- Після авторизації отримуєте 404
- URL: `https://admin.foryou-realestate.com/api/amo-crm/callback?code=...`

**Рішення:**
1. Перевірте, що роут `/api/amo-crm/callback` зареєстрований в `admin-panel-backend/src/server.ts`
2. Перевірте, що файл `admin-panel-backend/src/routes/amo-crm.routes.ts` існує

---

### Проблема: Токени зберігаються глобально (не для користувача)

**Симптоми:**
- Після підключення AMO CRM токени доступні всім користувачам
- Статус AMO CRM показує "Підключено" для всіх користувачів

**Рішення:**
1. Перевірте, що `POST /api/amo-crm/exchange-code` зберігає токени з `userId` з JWT
2. Перевірте, що `GET /api/amo-crm/status` перевіряє токени для поточного користувача

---

## 📋 Детальний чеклист тестування

Див. `AMO_CRM_TESTING_CHECKLIST.md` для повного чеклисту тестування.

---

## 🎯 Критерії успіху

1. ✅ OAuth flow працює від початку до кінця
2. ✅ Токени зберігаються для конкретного користувача
3. ✅ Статус AMO CRM оновлюється коректно
4. ✅ Відключення AMO CRM працює
5. ✅ Leads завантажуються після підключення

---

**Наступний крок:** Протестуйте OAuth flow в мобільному додатку!
